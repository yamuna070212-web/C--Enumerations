#include <stdio.h>

enum Day
{
    MON,
    TUE,
    WED,
    THU,
    FRI
};

int main()
{
    enum Day days[5] = {MON, TUE, WED, THU, FRI};

    for(int i = 0; i < 5; i++)
    {
        printf("%d ", days[i]);
    }

    return 0;
}
Output
0 1 2 3 4
